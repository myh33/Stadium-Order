## Packages
qrcode.react | Generating QR codes for order pickup
framer-motion | Smooth animations for cart, modals, and page transitions
lucide-react | Icons for UI elements (already in base, but emphasizing use)

## Notes
- Using placehold.co for mock food images as requested.
- Admin dashboard protected by simple auth check (simulated or real).
- Delivery fee logic needs to be handled in frontend cart calculation before sending.
- Polling interval for order status updates should be set to ~5-10 seconds.
